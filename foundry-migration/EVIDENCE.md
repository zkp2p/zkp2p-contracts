# Foundry Test Migration Evidence

This ledger is the durable source of truth for the Hardhat-to-Foundry migration requested in Codex source task `019f83af-c88b-76c3-824b-bc2f8eefea94`. The active goal is not complete until its pull request is merged and the merged state is verified on `main`.

## Governing gates

- Preserve one-to-one traceability from every hand-crafted Hardhat behavior to deterministic Foundry coverage before removing Hardhat tests.
- Establish the authoritative Hardhat coverage and timing baseline before deleting either suite.
- Rebuild existing Foundry tests from the Hardhat oracle before adding additive fuzz/property and stateful invariant assurance.
- Finish with zero skipped, pending, disabled, placeholder, log-only, event-gated, or silently omitted tests.
- Run deterministic, fuzz, invariant, integration, deployment, and any required pinned-fork coverage on every applicable CI run.
- Upload stable Foundry LCOV to Codecov through an observable required step.
- Strictly improve overall production line coverage and branch coverage when the baseline is below 100%; do not regress statements, functions, or unjustifiably regress per-file metrics; require 100% changed production coverage.
- Record fair cold/warm local benchmarks, CI wall time, isolation/repetition evidence, and mutation sensitivity.
- Preserve production bytecode, public APIs, storage layout, and protocol economics unless a genuine pre-existing bug requires a separately justified regression-tested fix.
- Use `gh` for GitHub operations and ordinary `git` for fetch/rebase/commit/push. Rebase whenever `origin/main` advances and immediately before merge. Never bypass protection or merge pending/failing checks.

## Repository baseline

| Field | Value |
| --- | --- |
| Repository | `zkp2p/zkp2p-contracts` |
| Isolated worktree | `/Users/sachin/.codex/worktrees/1a3b/zkp2p-v2-contracts` |
| Branch | `codex/foundry-test-migration` |
| Starting SHA | `a55b49db180ddf2dee96334bace4e38553cd0943` |
| Current rebased base SHA | `f6fd7aa4b475ea0045cddabe245c26584b960000` |
| Starting commit | `fix: harden deferred settlement accounting and integration (#189)` |
| Fetch command | `git fetch origin main --prune` |
| Initial worktree state | Clean detached worktree; branch created directly from fetched `origin/main` |
| Host | Apple M5 Max, 18 logical CPUs, 128 GiB RAM, macOS 26.4.1 arm64 |
| Git | 2.50.1 (Apple Git-155) |
| Node | 20.20.2 |
| Yarn | 4.9.1 from `.yarn/releases/yarn-4.9.1.cjs` |
| Foundry | 1.7.1, commit `4072e48705af9d93e3c0f6e29e93b5e9a40caed8` |
| GitHub CLI | 2.90.0 |
| Dependency install | `PATH=/opt/homebrew/opt/node@20/bin:$PATH node .yarn/releases/yarn-4.9.1.cjs install --immutable` (pass; 12.48s after correcting Node 24 environment mismatch) |

## Phase 0 inventory

- Runtime-expanded Hardhat inventory: 61 spec files, 977 suites, 1,514 named behaviors, 693 hooks, 531 imported dependency records, and 302 named local callables. Every source file is SHA-256 pinned in the inventory.
- After rebasing the relayer hard cut (`845f8a0`), the same runtime inventory contains 61 files, 977 suites, 1,517 named behaviors (1,512 executable and 5 pending), 693 hooks, 531 imported dependency records, and 302 named local callables. The committed inventory and manifest were regenerated from the rebased sources; the original 1,514-row state remains preserved in Git history.
- Initial canonical group: 36 files / 1,305 behaviors (1,300 executable, 5 pending). On current `845f8a0`, the canonical group is 36 files / 1,308 behaviors (1,303 executable, 5 pending). Patch-coverage remains 1 file / 33 executable behaviors, and deployment remains 24 files / 176 executable behaviors.
- Foundry test implementations under `test-foundry/`: 16.
- Production and test-support Solidity files under `contracts/`: 103 (exact coverage denominator will follow `.solcover.js`).
- Test entry points discovered in `package.json`: `test`, `test:integration`, `test:deploy`, `coverage`, `test:forge`, `test:forge:fuzz`, `test:forge:invariant`, `test:forge:fork`, `test:forge:coverage`, and package tests.
- Existing CI problems observed before changes: workflow branch allowlists omit ordinary feature branches; Foundry fuzz CI forces 100 runs below configured 256; fork tests are omitted on pull requests and depend on an RPC secret; Codecov upload is skipped when its token is absent; Foundry/action versions are unpinned; suite names and jobs split coverage in ways that can omit tests.
- `origin/main` advanced during Phase 0 from the starting SHA to `659fb603` (`feat: fund intent extensions with delegated stake (#190)`). The branch was immediately rebased. Upstream changed `RiskManager`, `StakeVault`, their Hardhat tests, and two old Foundry files; affected and complete baselines are being refreshed on the new base while retaining the original cold measurement.
- During deterministic translation, `origin/main` advanced again to `845f8a0` (`refactor: remove privileged relayer intent access (#192)`). The branch was immediately rebased. The V2/V3 relayer dependency and global multiple-intent switch were removed, ordinary V2 accounts gained concurrent intents, affected Foundry topologies were hard-cut to the new constructors, and the runtime oracle/manifest were regenerated rather than retaining compatibility aliases.
- After the first fully green PR runs, `origin/main` advanced to `f6fd7aa` (`refactor: inline OrchestratorV2 lifecycle into standalone OrchestratorV3 (#193)`). The branch was immediately rebased. Upstream restored the legacy relayer/multiple-intent gate to V2 and made V3 an independent full-lifecycle implementation. Its 61 new V3 behaviors, three replaced V2/deployment behaviors, and three strengthened V2 governance behaviors are reconciled one-to-one in [UPSTREAM_DELTA.md](./UPSTREAM_DELTA.md): 67 impacted behaviors mapped, zero omitted, plus five additive V3 branch assertions. The inherited parity fixtures deploy and execute the real standalone V3; no V2 instance receives V3 credit.

## Baseline commands and results

Measured results will be appended here before either test suite is deleted.

| Group | Exact command | Result/counts | Timing | Notes |
| --- | --- | --- | --- | --- |
| Hardhat cold canonical | `PATH=/opt/homebrew/opt/node@20/bin:$PATH node .yarn/releases/yarn-4.9.1.cjs clean`, then `PATH=/opt/homebrew/opt/node@20/bin:$PATH /usr/bin/time -p node .yarn/releases/yarn-4.9.1.cjs test` | 1,298 passing; 5 pending; exit 0; 124 Solidity files compiled | real 245.89s; user 182.27s; sys 12.10s | Clean generated state; canonical groups from `package.json` |
| Hardhat warm canonical | `PATH=/opt/homebrew/opt/node@20/bin:$PATH /usr/bin/time -p node .yarn/releases/yarn-4.9.1.cjs test >/dev/null` | Each run exit 0 with unchanged 1,298 passing / 5 pending registry | 101.67s, 100.06s, 100.85s; median 100.85s; range 1.61s | Generated artifacts retained; exact canonical script, output suppressed only to remove terminal rendering cost |
| Hardhat integration | `PATH=/opt/homebrew/opt/node@20/bin:$PATH /usr/bin/time -p node .yarn/releases/yarn-4.9.1.cjs test:integration` | Exit 1; no tests executed | real 0.18s; user 0.24s; sys 0.03s | Script references absent `test/integration/*.ts`; obsolete/silently empty baseline group must be resolved, not skipped |
| Hardhat deployment/default network | `PATH=/opt/homebrew/opt/node@20/bin:$PATH /usr/bin/time -p node .yarn/releases/yarn-4.9.1.cjs test:deploy` | 6 passing; 48 failing; exit 48 | real 3.41s; user 3.47s; sys 2.49s | Expected missing deployment artifacts when invoked without the CI deploy topology |
| Hardhat deployment/local node | Start `yarn chain`; run `yarn deploy:localhost`; then `yarn hardhat test $(find test/deploy -maxdepth 1 -type f -name '*.ts' -print \| sort) --network localhost` | Deploy exit 0; 176 passing / 0 pending / 0 failing; test exit 0 | deploy 9.11s; assertions 3.54s | Exact current CI topology; tracked localhost timestamp side effects restored after run |
| Hardhat canonical after rebase | `PATH=/opt/homebrew/opt/node@20/bin:$PATH /usr/bin/time -p node .yarn/releases/yarn-4.9.1.cjs test` | 1,300 passing; 5 pending; exit 0 | real 99.36s; user 41.67s; sys 9.36s | Current `659fb603` base; includes two upstream staking behaviors added after the original baseline |
| Hardhat relayer-cut affected groups | `yarn hardhat test test/orchestratorV2/orchestratorV2.spec.ts test/orchestrator/preIntentHook.spec.ts test/periphery/protocolViewerV2.spec.ts test/unifiedVerifier/unifiedPaymentVerifierV2.spec.ts test/unifiedVerifier/unifiedPaymentVerifierV3.spec.ts test/staking/riskManager.spec.ts test/hooks/whitelistPreIntentHook.spec.ts` | 152 passing; 0 pending; exit 0 | real 116.80s; user 110.09s; sys 5.08s | Clean compile on rebased `845f8a0`; covers every directly affected canonical source group |
| Hardhat canonical after relayer cut | `PATH=/opt/homebrew/opt/node@20/bin:$PATH /usr/bin/time -p node .yarn/releases/yarn-4.9.1.cjs test` | 1,303 passing; 5 pending; exit 0 | real 111.04s; user 47.43s; sys 10.07s | Full canonical oracle on rebased `845f8a0` |
| Hardhat affected staking after rebase | `yarn hardhat test test/staking/riskManager.spec.ts test/staking/stakeVault.spec.ts` | 125 passing; 0 pending; exit 0; 5 changed Solidity files compiled | real 27.14s | Targeted validation after rebasing upstream delegated-stake funding changes |
| Hardhat patch-coverage group | `yarn hardhat test test/patchCoverage/*.ts` | 33 passing; 0 pending; exit 0 | real 5.65s | Separately invoked coverage-only group; behaviors must move into ordinary named deterministic tests |
| Hardhat coverage | `PATH=/opt/homebrew/opt/node@20/bin:$PATH /usr/bin/time -p node .yarn/releases/yarn-4.9.1.cjs coverage` | 1,333 passing; 5 pending; 0 failing; exit 0 | real 870.61s; user 976.89s; sys 74.13s | Authoritative current-base parity baseline; canonical plus patch-coverage groups |
| Existing Foundry cold complete | `yarn test:forge` | 137 passing; 2 failing; 0 skipped across 15 suites / 139 tests; exit 1; 159 files compiled | compile 203.63s; real 207.89s; user 209.69s; sys 5.39s | Context only, not parity credit; default-profile live fork failures changed with live state |
| Existing Foundry warm complete | `yarn test:forge` | 137 passing; 2 failing; 0 skipped; exit 1 | real 2.07s | Same two live-fork failures: expected Across deposit but observed no spoke-pool increase/fallback |
| Existing Foundry fuzz | `yarn test:forge:fuzz` | 35 passing; 0 failing; 100 runs forced by script | real 0.51s | Script overrides the configured 256-run default downward |
| Existing Foundry invariants | `yarn test:forge:invariant` | 24 passing; 0 failing; 3 suites; 256 runs and 3,840 calls per invariant suite | real 1.45s | `fail_on_revert=false`; includes an empty invariant and log-only summaries; separate placeholder file is silently undiscovered |
| Existing Foundry fork | `FOUNDRY_PROFILE=fork yarn forge test --match-path 'test-foundry/fork/*Fork.t.sol' -vvv` | 3 passing; 0 failing against an unpinned live Base head; exit 0; 144 files compiled | compile 52.28s; real 54.84s | Default and fork-profile runs used different cached live blocks (`48917829` and `48917873`), demonstrating nondeterminism |
| Existing Foundry coverage | `yarn test:forge:coverage` | Exit 1 before tests | real 2.19s; user 1.35s; sys 1.14s | Forge coverage disables optimizer/IR and fails with `stack too deep` at `contracts/OrchestratorV2.sol:802`; no usable old-Foundry coverage artifact |
| Package build | `yarn build` | Pass | real 6.19s | Preserves compile and TypeScript release surface |
| Package tests | `yarn pkg:test` | 2 suites / 7 tests passing | real 2.56s | Package export/import smoke coverage |

### Baseline pending behaviors

These receive no exemption from migration and must become executable assertions:

1. `test/escrow/escrow.spec.ts` — `Escrow #pruneExpiredIntentsAndReclaimLiquidity when timestamp is after intent expiry should have called the orchestrator to prune intents`.
2. `test/orchestrator/orchestrator.spec.ts` — `Orchestrator #signalIntent when there aren't enough deposits to cover requested amount but there are prunable intents should prune the old intent and update the deposit mapping correctly`.
3. Same source/suite — `should delete the original intent from the intents mapping`.
4. Same source/suite — `should emit an IntentPruned event`.
5. Same source/suite under `when the reclaimable amount can't cover the new intent` — `should revert`.

### Existing Foundry confidence findings

- `test-foundry/invariant/V2RateFlowInvariantSkeleton.t.sol` is an explicit compile-safe placeholder. Its ten empty `spec_*` functions are deliberately undiscovered, so CI reports no failure or skip.
- `EscrowInvariant.invariant_FeeBounds()` has an empty body.
- `EscrowInvariant.invariant_callSummary()` and `OrchestratorInvariant.invariant_callSummary()` only print counters/accounting; they assert no property.
- Global `fail_on_revert = false` permits handler sequences to revert without failing. Call distributions show substantial reverting traffic, requiring handler-by-handler reachability and exercise review.
- The fork suite calls `vm.createSelectFork(rpcUrl)` without a block number and depends on mutable Base USDC/Across SpokePool state. It failed under the default profile and passed minutes later under the fork profile at a different live block.
- `test-foundry/fuzz/RiskManagerMathFuzz.t.sol` contains arithmetic-focused properties that require re-derivation against production contract behavior; no old fuzz assertion receives parity or additive-assurance credit.
- Repository scans found no `.only`, `xit`, or `xdescribe`; the five known Hardhat omissions are implemented through one `it.skip` and one `describe.skip` containing four tests.

## Phase 1 nuclear reset

After committing the full Hardhat baseline, coverage artifacts, runtime inventory, and 1,514-row manifest, all 16 pre-existing `.t.sol` implementations under `test-foundry/` were deleted. No test assertion, fixture, handler, fork implementation, fuzz property, invariant, or placeholder was retained. The independently versioned `lib/forge-std` dependency remains as generic test infrastructure; it contains no repository-specific test behavior and will underpin the re-derived suite.

## Coverage comparison

The authoritative baseline instruments all 35 production files selected by `.solcover.js`. Exact totals are:

| Metric | Covered / total | Baseline |
| --- | ---: | ---: |
| Statements | 1,751 / 1,775 | 98.65% |
| Branches | 1,600 / 1,770 | 90.40% |
| Functions | 440 / 446 | 98.65% |
| Lines | 2,327 / 2,379 | 97.81% |

Machine-readable evidence is committed in [hardhat-coverage-summary.json](./baseline/hardhat-coverage-summary.json) and [hardhat-coverage-by-file.csv](./baseline/hardhat-coverage-by-file.csv). The extraction tool records SHA-256 hashes of the original `coverage-final.json` and LCOV inputs and reproduces the report totals.

Final Foundry coverage is generated by `corepack yarn coverage` from the same authoritative 35 production files. A full `--ir-minimum` execution retains the denominator and runs every test; exact-source standard-compiler shards recover mappings that Solidity 0.8.18 minimal IR cannot anchor. The runner also reconstructs the 1,512 behaviors that executed in the starting Hardhat run from the immutable inventory and manifest. Those rows resolve to 1,383 exact live Foundry destinations. An inverse-name run plus a contract-scoped collision-recovery run select all of them and six conservative same-name additions (1,389 total); a live `forge test --list --json` preflight proves the actual filter union has no missing or unexpected destination.

Strict improvement is enforced only on the identical Foundry minimal-IR denominator. Istanbul and Foundry use different statement/branch models, so directly subtracting their percentages would be invalid. The historical Hardhat totals remain preserved as absolute evidence, while the auditable starting-behavior bridge is the acceptance baseline:

| Metric | Starting-behavior bridge | Complete Foundry minimal IR | Enforced result |
| --- | ---: | ---: | ---: |
| Lines | 2,714 / 2,947 (92.09%) | 2,910 / 2,947 (98.74%) | +196 anchors / +6.65 pp |
| Statements | 2,965 / 3,307 (89.66%) | 3,251 / 3,307 (98.31%) | +286 / +8.65 pp |
| Branches | 455 / 801 (56.80%) | 588 / 801 (73.41%) | +133 paths / +16.61 pp |
| Functions | 430 / 458 (93.89%) | 458 / 458 (100.00%) | +28 / +6.11 pp |

The two bridge partitions expose statement totals but not stable statement IDs, so their combined statement hit count is the conservative maximum rather than an unsafe sum. The complete execution is structurally a strict test superset. The gate additionally rejects every per-production-file regression across all four metrics. Exact-source mapping shards do not participate in the improvement comparison; they only recover current-suite anchors after that gate passes.

| Historical/current absolute context | Lines | Statements | Branches | Functions |
| --- | ---: | ---: | ---: | ---: |
| Starting Hardhat/Istanbul | 2,327 / 2,379 (97.81%) | 1,751 / 1,775 (98.65%) | 1,600 / 1,770 (90.40%) | 440 / 446 (98.65%) |
| Final mapped Foundry LCOV | 2,930 / 2,947 (99.42%) | 3,269 / 3,307 (98.85%) | 764 / 801 (95.38%) | 458 / 458 (100.00%) |

The corrected coverage run passed 1,528 tests with zero failures/skips. Its measured components were 281.99s for the complete minimal-IR execution, 12.34s across 16 accurate-map shards, 272.23s for the main parity bridge, and 273.13s for collision recovery: 839.69s measured shard sum and approximately 840.82s clean-equivalent wall clock. The latter replaces a 282.69s selector-diagnostic shard inside the original 851.28s uninterrupted run; CI supplies the final uninterrupted clean measurement. Every invocation uses fixed seed `0x00...01`, so fuzz/invariant paths and totals are reproducible. The runner independently discovers the current production source set and rejects any source without an LCOV record; a temporary `contracts/CoverageDenominatorSentinel.sol` was correctly rejected by the merge gate before being deleted. Duplicate LCOV records for independently linked source topologies are merged cumulatively by anchor instead of silently retaining only the final record. `coverage/lcov.info`, the per-file CSV, and the complete summary JSON are reproducibly generated; the committed [final summary](./final/foundry-coverage-summary.json) records the acceptance totals. No production Solidity file differs from `origin/main`, so changed-production patch coverage is vacuously 100%; Codecov independently enforces 100% patch coverage if a production file changes.

Ten metric-specific source-map exceptions remain across six files. Each is enforced in `coverage-exceptions.json`, is rejected if stale, and is backed by standard-compiler stack-too-deep output or explicit minimal-IR anchor warnings. The upstream-expanded V3 exceptions cover only its executed pause/unpause wrapper anchors: exact owner/non-owner tests pass and V3 branch/function coverage is 100%, while standard coverage fails stack-too-deep in `FeeSettlementLib`. Exceptions affect only exact-source mapping recovery; the identical-denominator aggregate and per-file gates admit no exception. Production code was not refactored merely to satisfy instrumentation.

## Parity status

The machine-auditable [migration manifest](./hardhat-to-foundry-manifest.csv) contains all 1,517 runtime-expanded named behaviors captured on base `845f8a0`, with unique stable hash IDs, source file, suite path, scenario, expected-behavior category, inherited hook/helper dependencies, one-to-one translation shape, and baseline status. The underlying [Hardhat inventory](./baseline/hardhat-inventory.json) preserves suites, hooks, imports, named local callables, source hashes, group totals, and the five pending cases. Runtime enumeration expands parameterized test construction rather than relying on a text-only `it()` search. No row is verified until its Foundry destination passes alone and in the complete repeated suite.

The 1,517-row manifest remains the immutable oracle captured before Hardhat removal on base `845f8a0`. Upstream `f6fd7aa` subsequently changed that deleted source surface; the committed [upstream delta](./UPSTREAM_DELTA.md) reconciles all 61 added V3 behaviors, three replaced V2/deployment behaviors, and three strengthened V2 governance behaviors against exact Foundry destinations. The upstream Hardhat named-behavior total would be 1,576 after accounting for two deleted V2 cases and 61 new V3 cases. The standalone V3 suites pass 66/66 independently. The rebased complete deterministic suite passes 1,504/1,504 with zero failures/skips after a cold 808.26s compile of the expanded 95-file production/test graph.

| Re-derived domain | Hardhat rows mapped | Foundry tests | Isolation evidence | Status |
| --- | ---: | ---: | --- | --- |
| Seven registries | 120 | 50 across 7 contracts | Every test passed individually; complete file 50/50, 0 skipped; warm real 0.22s | Independently verified; shadow/full-repeat gates pending |
| Chainlink and Pyth oracle adapters | 27 | 25 across 2 contracts | Every test passed individually; complete file 25/25, 0 skipped; warm real 0.22s | Independently verified; shadow/full-repeat gates pending |
| Simple and multi attestation verifiers | 37 | 24 across 2 contracts | Every test passed individually; complete file 24/24, 0 skipped; cumulative deterministic suite 99/99, warm real 0.22s | Independently verified; shadow/full-repeat gates pending |
| Threshold signature library | 22 | 16 | Every test passed individually; complete file 16/16, 0 skipped; cumulative deterministic suite 115/115, warm real 0.21s | Independently verified; shadow/full-repeat gates pending |
| Base unified verifier configuration | 19 | 8 | Every test passed individually; complete file 8/8, 0 skipped; cumulative deterministic suite 123/123, warm real 0.29s | Independently verified; shadow/full-repeat gates pending |
| Unified payment verifier V3 core | 11 | 11 | Every test passed individually; complete file 11/11, 0 skipped; cumulative deterministic suite 134/134, warm real 0.30s | Independently verified against both legacy- and V2-shaped caller interfaces; actual Orchestrator V2/V3 shadow integration gate pending |
| Unified payment verifier core | 19 | 12 | Every test passed individually; complete file 12/12, 0 skipped; cumulative deterministic suite 146/146, warm real 0.21s | Independently verified; actual Orchestrator settlement shadow integration gate pending |
| Unified verifier / OrchestratorV2 compatibility | 1 | 1 | End-to-end test passed independently and in the cumulative deterministic suite; cumulative 147/147, warm real 0.21s | Verified with real EscrowV2 deposit/lock, OrchestratorV2 signal/fulfill, verifier event/nullifier, intent deletion, and exact token settlement |
| OrchestratorV2 delegated rates, manager fees, and relayer hard cut | 6 | 6 | Every test passed individually after rebase; complete file 6/6, 0 skipped; 42/42 rebased affected tests passed individually; cumulative deterministic suite 220/220; warm real 0.23s | Independently verified with real EscrowV2, delegated RateManager, signal-time fee snapshot, exact custom-error arguments, fulfillment/token accounting, ordinary-account concurrent intents, and absence of all four retired runtime selectors |
| EscrowV2 / Pyth oracle integration | 5 | 5 | Every test passed individually; complete file 5/5, 0 skipped; cumulative deterministic suite 156/156, warm real 0.21s | Independently verified with validated/stored config, rounded spread, fixed-rate floor, stale-price halt, fresh price updates, inline creation, and decoded event payload |
| EscrowV2 currency-rate lifecycle | 12 | 12 | Every test passed individually; complete file 12/12, 0 skipped; cumulative deterministic suite 168/168, warm real 0.26s | Independently verified fixed/oracle max, positive and negative spread rounding, stale halt, deactivation event/state cleanup, and explicit reactivation by either source |
| ProtocolViewerV2 aggregation | 12 | 12 | Every test passed individually; complete file 12/12, 0 skipped; cumulative deterministic suite 180/180, warm real 0.24s | Independently verified deposit/currency views, delegated-rate fallback/disable semantics, multi-deposit ordering, account/hash intent aggregation, nested deposit resolution, and exact invalid-address errors |
| Legacy ProtocolViewer aggregation | 15 | 15 | Every test passed individually; complete file 15/15, 0 skipped; cumulative deterministic suite 195/195, warm real 0.27s | Independently verified constructor wiring/errors, deposit/payment-method views, nonexistent records, ordered batch/account intent views, nonzero EIP-191 gating authorization, and expired-intent reclaimable liquidity |
| OrchestratorV2 pre-intent hooks | 23 | 23 | Every test passed individually; complete file 23/23, 0 skipped; cumulative deterministic suite 218/218, warm real 0.24s | Independently verified depositor/delegate administration, exact authorization errors, ephemeral-vs-persisted data, atomic rejection, removal/no-hook behavior, reentry containment, per-deposit signer governance, EIP-191 signature binding, expiry, signer absence, and direct-call rejection |
| Whitelist pre-intent hook | 29 | 29 | Every test passed individually; complete file 29/29, 0 skipped; cumulative deterministic suite 249/249, warm real 0.21s | Independently verified constructor wiring, depositor/delegate authorization, exact errors/events, atomic whitelist batches, removal isolation, dedicated hook administration, direct-call rejection, real signaling, independent dual-hook storage/execution/removal, and atomic rollback when whitelist validation rejects |
| Legacy and V2 Across bridge hooks | 56 | 56 across 2 contracts | Every legacy and V2 test passed individually; complete file 56/56, 0 skipped; cumulative deterministic suite 305/305, warm real 0.20s | Independently verified distinct fixed-orchestrator and registry-authorized paths, exact structural/payload errors, complete Across parameter forwarding, boundary output viability, both fallback reasons and source-chain settlement, approval cleanup, bytes32 conversion, owner-only ERC20/native rescue, partial rescue, failed native transfer, and receive behavior |
| RateManagerV1 | 50 | 50 | Every test passed individually; complete file 50/50, 0 skipped; cumulative deterministic suite 355/355, warm real 0.21s | Independently verified manager creation/config/fees, exact authorization and cap errors, single and batch rate updates with dimensional validation, default views, minimum-liquidity lifecycle, real EscrowV2 opt-in liquidity reads, escrow-registry authorization modes, and owner-only registry replacement |
| EscrowV2 rate-manager delegation | 21 | 21 | Every test passed individually; complete file 21/21, 0 skipped; cumulative deterministic suite 376/376, warm real 0.22s | Independently verified depositor-only delegation, callback/event/config state, rollback on rejected opt-in, CEI resistance to reentrant opt-in, clearing, native/delegated/floor/disabled/stale effective-rate precedence, and delegated-fee fallback |
| EscrowV2 oracle-rate configuration | 29 | 29 | Every test passed individually; complete file 29/29, 0 skipped; cumulative deterministic suite 405/405, warm real 0.21s | Independently verified inline oracle creation and zero floors, empty-config omission, signed spread boundaries/rounding, fixed-floor precedence, stale/invalid/future/reverting oracle halts, delegate authorization, normalized-config length, removal, nested batch dimensions, conditional oracle mutation/removal events, multi-currency deactivation, and unsupported/inactive tuple errors |
| OrchestratorV2 lifecycle, hooks, governance, and validation edges | 55 | 55 across 2 domain contracts and 1 abstract fixture | Every test passed individually; complete files 55/55, 0 skipped; same-commit Hardhat source 55/55 in real 9.28s; cumulative deterministic suite 460/460, warm real 0.21s | Independently verified cancellation/unlocking, exact manual-release fee accounting, release/fulfill reentrancy guards, fulfill validation, escrow-led and orphan pruning, hook setter/execution context, governance, ordinary multi-intent behavior, referral validation/distribution, payment/currency/post-hook validation, exact-pull balance invariants, and caller-bound EIP-191 gating signatures. The shared fixture recreates the Hardhat production topology and contains no test cases. |
| EscrowV2 legacy management, lifecycle, oracle/delegation edges, governance, and views | 70 | 70 across 3 domain contracts and 1 abstract fixture | Every test passed individually; complete files 70/70, 0 skipped; same-commit Hardhat source 70/70 in real 9.73s; cumulative deterministic suite repeated 530/530 with zero skips in real 0.23s, 0.23s, and 0.26s (median 0.23s) | Independently verified deposit creation/funding/withdrawal and configuration, expired-intent reclamation with per-orchestrator pruning and rollback, locking/unlocking/partial settlement/dust, guardian expiry, fixed/oracle/delegated-rate fail-safe paths, all owner configuration, pause state, and getter consistency. The shared fixture recreates the Hardhat topology and contains no test cases. |
| EscrowV2 branch and negative-path coverage | 108 | 108 across 4 domain contracts | Every test passed individually; complete files 108/108, 0 skipped; same-commit Hardhat source 108/108 in real 11.11s; cumulative deterministic suite repeated 638/638 with zero skips in real 0.28s, 0.28s, and 0.27s (median 0.28s) | One-to-one verified authorization on every guarded entry point, array/input/oracle/rate-manager validation, owner controls, lock/unlock/guardian errors, oracle halts, retain/close behavior, idempotence, all paused entry points, and the explicit reentrancy-guard branch. Mechanical audit proved 108 unique source rows map to 108 unique existing Solidity tests. |
| Complete legacy Escrow source | 276 of 276 | 276 across 15 domain contracts and 1 abstract full-topology fixture | Every translated test passed individually; final same-commit full Hardhat source 275 passing plus 1 pending in real 30.27s; the pending prune-callback assertion is implemented and passing in Foundry; cumulative deterministic suite 914/914 with zero skips, warm real 0.23s, 0.21s, and 0.22s (median 0.22s) | One-to-one verified the entire source: deposit/configuration lifecycle, authorization and exact errors/events, permissionless expiry pruning and callback payloads, lock accounting/bounds/duplicate prevention/reclamation/reentry containment/max-intent behavior, cancellation/automatic pruning through the real Orchestrator, unlock accounting, full and partial settlement, retain-on-empty closure, dust sweeping, aggregate guardian expiry caps, every owner setter, pause transitions, and expired-intent views. The fixture recreates the production Escrow/Orchestrator/registries/verifier/viewer topology and contains no tests. Independent re-enumeration mechanically proved all 276 source rows map to 276 unique existing Solidity tests; zero rows remain unmapped. |
| Complete legacy Orchestrator source | 141 of 141 | 141 across 10 domain contracts and 1 abstract full-topology fixture | Every translated test passed individually; final same-commit full Hardhat source 137 passing plus 4 pending in real 20.77s; all four pending expiry-reclamation cases execute and pass in Foundry; cumulative deterministic suite 1,055/1,055 with zero skips | One-to-one verified the entire source: constructor wiring, signal validation/reclamation, cancellation cleanup, verifier release math, min/rate snapshots, close/config deletion, fee conservation, negative verification precedence, post-hook settlement/reentry behavior, partial payment, pause behavior, depositor-only manual release, manual close cleanup, protocol/referrer fee conservation, hook bypass on manual release, pruning across zero/unknown hashes/wrong callers/multiple accounts/empty arrays, every governance setter and boundary, pause ownership, and account-intent views/order. Independent re-enumeration mechanically proved all 141 source rows map to 141 unique existing Solidity tests; zero rows remain unmapped. |
| Complete StakeVault source | 56 of 56 | 56 across 5 domain contracts and 1 abstract fixture | Every translated test passed individually; complete files 56/56, 0 skipped; full same-commit Hardhat source 56/56 in real 2.72s; cumulative deterministic suite 1,111/1,111 with zero skips | One-to-one verified the entire source: exact deposits/liabilities/events, delegated ownership/revocation/anti-squatting/preapproval/batch atomicity, reservation uniqueness/free-stake accounting/update/increase/release, pause/exit admission gates including zero-increment refreshes, partial slashing and aggregated compensation, partial/full withdrawal exclusion/delay/reservation behavior, deferred gross custody and fee vesting/rounding/aggregation/slashing, and delayed controller handover with snapshotted position authority. Independent re-enumeration mechanically proved all 56 source rows map to 56 unique existing Solidity tests; zero rows remain unmapped. |
| Complete RiskManager hard-cut branch source | 18 of 18 | 18 across 3 domain contracts and 1 abstract harness fixture | Every translated test passed individually; complete files 18/18, 0 skipped; full same-commit Hardhat source 18/18 in real 0.97s; cumulative deterministic suite 1,129/1,129 with zero skips | One-to-one verified constructor dependency validation, governance and config boundaries, zero/rounded formulas, direct-call/admission/guardian/timestamp/cancellation recovery branches, all three risk modes, duplicate/token/collateral/exit rejection, settlement context validation, stake-backed zero-consumption accounting, deferred gross custody and payout-owner snapshot, unbonded terminal state, and the half-open maturity boundary. The harness topology is the Hardhat source's deliberate unit boundary; real OrchestratorV3 integration remains separately mapped from `riskManager.spec.ts`. |
| RiskManager real-system configuration, admission, extensions, settlement, chargebacks, and OrchestratorV3 boundaries | 70 of 70 | 70 across 10 domain contracts and 2 abstract real-system fixtures | Every translated test passed individually; complete files 70/70, 0 skipped; same-commit full Hardhat source 70/70 (real 2.94s); cumulative deterministic suite 1,199/1,199 with zero skips | Complete one-to-one parity now covers custody/config/formulas, admission/delegation/snapshots, extension accounting and boundaries, exact stake-backed and deferred settlement, EIP-712 chargebacks, replay/payment binding, half-open maturity, contingent fee custody and slashing, manual/ordinary routing, ten-referral gas limits, partial/overpull and atomic rollback, lost-code recovery, cancellation acknowledgement, governance, hook snapshots, and guarded lifecycle reentry. Zero source rows remain. |
| Legacy system deployment wiring | 21 of 21 | 21 in 1 real-constructor topology contract | Complete file 21/21, 0 skipped; same-commit localhost Hardhat source 21/21 in real 1.48s after the legacy deployment segment; cumulative deterministic suite 1,220/1,220 with zero skips | One-to-one verified local-network owner fallbacks, registry ownership, Escrow parameters/registry/orchestrator wiring, Orchestrator registries/fee configuration, and ProtocolViewer immutables. A clean all-script localhost deployment now reaches this valid legacy state but later fails in `14_deploy_v2_system.ts` because that script passes seven arguments to the current six-argument `OrchestratorV2`; this pre-existing deployment drift is retained as an explicit remediation item for the V2 deployment block rather than hidden or weakened. |
| Legacy unified-verifier and payment-method deployment wiring | 45 of 45 | 46 across 2 real-constructor/configuration contracts | Every Foundry test passed together; same-commit localhost Hardhat sources 45/45 in real 1.60s; cumulative deterministic suite 1,266/1,266 with zero skips | One-to-one verified owner/dependency/nullifier-permission outcomes, exact registry currency ordering for ten retained platforms, generic Zelle, N26/Luxon removal from both surfaces, and all verifier membership. The formerly commented orchestrator-registry constructor assertion is restored as an additional executable Foundry test. The clean deployment used for the shadow run now completes in real 8.48s after the separately committed removal of `14_deploy_v2_system.ts`'s obsolete relayer constructor argument. |
| Across and V2 core deployment wiring | 26 of 26 | 26 across 2 real-constructor topology contracts | Every Foundry test passed together; same-commit localhost Hardhat sources 26/26 in real 1.88s; cumulative deterministic suite 1,292/1,292 with zero skips | One-to-one verified legacy Across token/orchestrator/spoke-pool wiring, ownership and hook registration; OrchestratorRegistry dual-version membership; EscrowV2 configuration and whitelist; OrchestratorV2 dependency/fee configuration and retired-ABI removal; UnifiedPaymentVerifierV2 dependencies and one-way legacy-nullifier permission revocation. |
| V2 periphery, Pyth, multi-witness, and Safe batch deployment | 24 of 24 | 24 across 4 focused parity contracts | Every Foundry file and all 24 tests passed independently; same-commit localhost Hardhat sources 24/24 in real 1.46s; cumulative deterministic suite 1,316/1,316 with zero skips | One-to-one verified periphery constructor/ownership/registry wiring, local Pyth topology, configured multi-witness membership/threshold/ownership/UPV-V2 wiring, and the real TypeScript `SafeBatchCollector` case-insensitive deduplication through Foundry FFI rather than a test-only Solidity imitation. |
| V2 payment-method and generic-Zelle cutover | 45 of 45 | 45 in one real legacy/V2/V3 topology contract | The 40 V2 tests passed as isolated invocations and all 45 passed together; same-commit localhost Hardhat sources passed 40/40 in real 1.59s and 5/5 in real 1.40s; cumulative deterministic suite 1,361/1,361 with zero skips | One-to-one verified exact ten-method membership and currencies, registry routing to a real active V3 verifier, retained V2-verifier membership, canonical generic-Zelle hashing/routing, and removal of N26/Luxon from all three surfaces. |
| Risk-settlement deployment topology | 12 of 12 | 12 in one fresh linked deployment contract | All 12 Foundry tests passed together and as isolated invocations; same-commit localhost Hardhat source 12/12 within the 15-test deployment shadow run (real 2.67s); cumulative deterministic suite 1,373/1,373 with zero skips | One-to-one verified linked production components, retired-hook ABI/source/type removal, vault/manager/nullifier wiring, six ownership transfers, independent payment/risk witness domains, one-way V3 nullifier and routing cutover, OrchestratorV3 admission surface, timing parameters, three platform policies, and fail-closed production policy lookup. FFI is limited to the two source/TypeScript policy assertions. |
| Patch-coverage behaviors | 33 of 33 | 33 exact named destinations across normal domain suites | Same-commit Hardhat source 33/33 in real 6.43s; all new Foundry branch tests passed independently and in their full files; cumulative deterministic suite 1,396/1,396 with zero skips | Replaced coverage-only grouping with normal semantic tests for accept-all routing, zero-delegate creation, oracle halt/future/revert handling, delegated rates, retain/outstanding lifecycle, maximum-intent reclaim, zero-fee configuration, no-fee fulfillment, pruning, and every explicitly forced legacy/V2 reentrancy-guard branch. Existing exact tests are reused only where their assertions cover the complete source behavior. |
| Legacy-Zelle removal deployment | 3 of 3 | 3 in one real legacy/V2/V3 topology contract | All three Foundry tests passed as isolated invocations and together; same-commit localhost Hardhat source 3/3 within the 15-test deployment shadow run (real 2.67s); cumulative deterministic suite 1,399/1,399 with zero skips (warm real 1.04s) | One-to-one verified registry-first removal from all three legacy surfaces, exact nine-call Safe-owned batch order/calldata/value and duplicate suppression through the real TypeScript deployment helper, preservation and withdrawal of deposits using removed methods, rejection of new legacy deposits/intents, successful generic-Zelle proof fulfillment, idempotent migration, and final generic-V3/legacy-unsupported deployed state. |

Historical translation checkpoint: 1,469 / 1,517 rows (96.84%); 48 remained at that point. The complete rebased deterministic suite was 1,361/1,361 with zero skips; all 276 legacy Escrow tests, all 141 legacy Orchestrator tests, all 56 StakeVault tests, all 18 RiskManager branch tests, all 70 real-system RiskManager/OrchestratorV3 tests, the then-current 161 deployment source rows plus the restored commented deployment assertion, the 108 EscrowV2 branch tests, the earlier 70 EscrowV2 legacy tests, and the earlier 42 constructor/control-affected V2 tests passed as isolated invocations. Consolidations combine assertions from the same Hardhat setup/action only where the manifest gives every source behavior an exact Foundry function destination; completed sources and current slices are physical one-to-one translations. Completed slices additionally cover immutable verifier registries, attestation-verifier rotation, payment-method membership/enumeration, exact owner/error/event behavior for the base unified verifier, V1/V3 EIP-712 attestation integrity, nullifier replay, V3 bidirectional payment-to-intent bindings, snapshot validation, overpayment capping, verifier rotation, real OrchestratorV2 settlement, delegated effective-rate enforcement, manager-fee bounds/snapshotting, net settlement accounting, the relayer/multiple-intent hard cut, fixed/Pyth/static-oracle effective-rate behavior including source lifecycle/staleness/signed spread rounding, both legacy/V2 ProtocolViewer aggregation, OrchestratorV2 hook and gating edges, Across hooks, RateManagerV1, complete Escrow/Orchestrator/StakeVault/RiskManager parity, complete RiskManager hard-cut branch parity, actual OrchestratorV3 settlement/control/recovery parity, legacy/V2 core deployment topology, V2 periphery/Pyth/multi-witness wiring, real Safe-batch deduplication behavior, and the ten-method V2-to-V3 verifier and generic-Zelle cutover.

The final two deployment sources were shadow-run together on the same commit and fully deployed localhost state: `test/deploy/27_removeLegacyZellePaymentMethods.spec.ts` passed 3/3 and `test/deploy/28_riskSettlementSystem.spec.ts` passed 12/12 (15/15 total, real 2.67s). Risk-settlement parity is now complete; the three legacy-Zelle removal rows remain deliberately pending until the exact registry-first Safe batch and withdrawal/generic-payment continuity assertions have executable Foundry destinations.

Subsequent checkpoint: 1,481 / 1,517 manifest rows are verified (97.63%); 36 remain. The complete deterministic suite is 1,373/1,373 with zero skips. All 12 risk-settlement deployment rows now have independent executable destinations; only the three legacy-Zelle removal rows and 33 patch-coverage rows remain.

Patch-coverage checkpoint: 1,514 / 1,517 manifest rows are verified (99.80%); three legacy-Zelle removal rows remain. The complete deterministic suite contains 1,396 tests with zero skips. The 33-row source was re-enumerated in inventory order and reconciled one-to-one to exact named destinations; it is ready to be removed with the rest of the Hardhat suite after the final deployment source passes its parity gate.

Deterministic translation checkpoint: all 1,517 / 1,517 manifest rows are verified (100%); zero remain. The complete deterministic suite contains 1,399 tests with zero skips and passed in warm real 1.04s. The final legacy-Zelle source's three behaviors passed independently and together with exact real-helper Safe-batch evidence. Deterministic translation is complete; the remaining Phase 2 work is the final same-commit shadow repeat, mechanical omission/reconciliation audit, flake/isolation gates, parity report, and removal of the obsolete Hardhat test surface.

Final Phase 2 parity gate: on exact branch commit `405a395`, the canonical Hardhat oracle passed 1,303 with the same five recorded pending cases (96.14s), patch coverage passed 33/33 (5.47s), and a fresh localhost deployment passed all 176 deployment assertions (8.21s deploy + 3.07s tests). Every deterministic Foundry file passed independently, followed by three complete 1,399/1,399 zero-skip runs in 1.07s, 1.09s, and 1.08s (median 1.08s). The independent audit verified all 61 source hashes, all 1,517 inventory/manifest rows, 1,388 live mapped destinations, zero unmapped/missing rows, 77 explicit consolidations, zero splits, all five pending resolutions, and 11 additive deterministic tests. The committed [parity report](./PARITY_REPORT.md) records the gate and authorizes removal of Hardhat-only testing assets while retaining deployment/build/release infrastructure.

## Fuzz and invariant catalog

Phase 3 is additive to the original locked 1,399-test deterministic parity suite and the 65-row upstream reconciliation; none of these tests receives parity credit.

### Property fuzzing

The canonical profile runs every property with 512 cases (`[fuzz].runs = 512`) and a 65,536 reject ceiling. All 14 properties passed with three explicit full-suite seeds (`0x01`, `0x02`, and `0x03`), for 21,504 generated property cases across the three repeated runs. The original 12-property set also passed the independent wide seeds `0x1234...cdef` and `0xffff...ffff`. Each property calls production contracts and records its added risk directly in NatSpec:

| Domain | Properties beyond deterministic parity |
| --- | --- |
| `OrchestratorV2FlowFuzz` | Exact value conservation across arbitrary bounded intent amounts, protocol/referral fees, direct recipients and post-hook routing; wrong-actor cancellation rejection and exact liquidity/index cleanup; both out-of-range amount boundaries; automatic expired-intent reclamation and lifecycle-index consistency. |
| `StakeVaultAccountingFuzz` | Stake/reservation/compensation/token conservation under arbitrary partial slashes; deferred gross/net/fee conservation including rounding; active-reservation exit blocking; delegated taker use without transferring withdrawal ownership. |
| `RegistryNullifierFuzz` | Complete payment-method removal across membership/enumeration/currency surfaces for arbitrary keys; bidirectional payment-to-intent uniqueness; legacy-nullifier authority after the one-way V2 cutover; owner/writer authorization across registries. |
| `BoundedCallFuzz` | The production assembly helper preserves success/revert classification, caps payloads exactly, and retains the returned prefix across 0–4,096-byte payloads and 0–512-byte caps. A fixed over-cap case executes on every fuzz iteration. |
| `RiskManagerDefensiveFuzz` | Corrupted settled positions in either non-chargeback risk mode revert with exact arguments before external attestation validation or stake mutation and retain their terminal state. |

Representative command: `forge test --match-path 'test-foundry/fuzz/**/*.t.sol' --fuzz-seed <32-byte-seed>`. Each current seeded run passed 14/14 with zero skips at 512 cases/property.

### Stateful invariants

The canonical profile uses 128 runs at depth 64 with `fail_on_revert = true` and call metrics enabled. Expected negative paths are caught and counted inside the handlers, so any unexpected outer handler revert fails the invariant run. Two independent handler systems exercise multiple actors and real production transitions:

| System | Stateful actions | Ghost model and asserted properties |
| --- | --- | --- |
| `StakeVault` | 3 funded stakers and 2 makers drive deposits, reserve/increase/update/release/slash, compensation claims, partial withdrawals, full exits, time advances, and unauthorized reserve attempts over 12 position slots. | Tracks per-staker stake/reservations, per-maker compensation, and aggregate token inflow/outflow. Asserts vault balance equals total liabilities and ghost net flow; aggregate/per-actor accounting and every live reservation agree with production state; exit/withdrawal states remain legal; compensation ownership is conserved; no unauthorized controller operation succeeds. |
| `NullifierRegistryV2` + legacy registry | Arbitrary binding collisions, legacy-first consumption followed by V2 attempts, and unauthorized writes across 16 payment and intent slots. | Tracks both binding directions and legacy consumption. Asserts bijective V2 bindings, replay prevention, legacy authority after cutover, complete success/revert accounting, and zero unauthorized writes. |

For every invariant, each seed executed 128 × 64 = 8,192 stateful calls. Across the three explicit seeds, all 12 StakeVault selectors were exercised 622–748 times per run and all three nullifier selectors 2,681–2,814 times per run; Forge reported zero uncaught reverts. The handler predicts the exact permitted custom-error selector for every modeled rejection and fails closed on an unexpected revert or unexpected success. Dedicated deterministic tests force every handler action through successful/expected-revert branches and inject both unexpected outcomes to prove that classification cannot silently absorb a regression. All ten invariant, reachability, and fail-closed tests passed under each seed. After the `f6fd7aa` upstream reconciliation and coverage-review hardening, the complete suite contains 1,528 tests (1,504 deterministic, 14 fuzz properties, 6 invariants, and 4 handler reachability/classification tests), all passing with zero skips. The two added properties exercise the real bounded-return helper across success/revert payloads and fail closed on every invalid risk-position mode before chargeback validation or stake mutation.

## Final local benchmarks

All local measurements use the baseline host/tool versions above. Warm distributions discard no samples; the first deterministic invocation after coverage changed the compiler configuration and is reported separately as a recompilation event.

| Measurement | Result | Configuration |
| --- | ---: | --- |
| Hardhat cold end-to-end | 245.89s | clean artifacts; compile + 1,298 passing / 5 pending baseline suite |
| Hardhat warm canonical | 100.06s, 100.85s, 101.67s; median 100.85s | compiled artifacts |
| Old Foundry cold | 207.89s | 137 passing / 2 live-fork failures; 159 files |
| Old Foundry warm | 2.07s | same two failures |
| Final Foundry cold end-to-end | 913.66s | clean `out`/`cache_forge`; 908.37s compile + complete pre-review 1,526-test execution; 242 files |
| Final deterministic warm | 0.80s, 0.80s, 0.80s; median 0.80s | 1,504 deterministic tests |
| Final complete warm | 5.14s, 5.58s, 5.84s; median 5.58s | 1,528 total: deterministic + 14×512 fuzz cases + 6×8,192 invariant calls + 4 handler reachability/classification tests; seeds 1, 2, and 3 |
| Final coverage | 839.69s measured shard sum; ~840.82s clean-equivalent wall | 281.99s complete minimal IR + 12.34s mapping + 272.23s main bridge + 273.13s collision bridge; fixed seed |

The production-like cold via-IR build is slower than Hardhat on the Apple M5 Max; warm execution and coverage are substantially faster. CI pins the toolchain and runs build/deploy and coverage in separate jobs. GitHub Actions wall time will be recorded from the PR before merge; cache state is never required for correctness.

## Mutation audit

Six temporary critical-behavior mutants were introduced one at a time with `apply_patch`, each focused test was required to fail, and each mutant was immediately reversed. No mutant is committed.

| Risk | Temporary mutation | Detecting test and observed failure |
| --- | --- | --- |
| Authorization | Inverted `StakeVault.onlyController` | `test_ReservationMutationRejectsNonController` failed because the unauthorized call no longer reverted |
| Accounting | Stopped crediting `_amount` to maker compensation | `test_MakerWithdrawsCreditedCompensation` failed with `ZeroAmount()` |
| Lifecycle | Deleted only intent amount instead of the complete `Orchestrator.Intent` | `test_CancelIntentDeletesIntent` failed because the owner remained nonzero |
| Fees | Forced legacy Orchestrator protocol fee to zero | `test_FulfillIntentProtocolFeeTransfersNetAndFee` failed on the exact recipient balance |
| Nullifier uniqueness | Suppressed the intent-to-nullifier half of the V2 binding | `test_NewBindingIsAtomicBidirectionalImmutableAndEmits` failed on the reverse mapping |
| Expiry safety | Reversed the legacy Escrow expiry-pruning comparison | `test_PruneAfterExpiryRemovesIntent` failed because the expired intent remained |

After all reversions, `git diff --name-only origin/main -- contracts` was empty and the six focused detecting tests passed together (6/6, zero skips). This confirms suite sensitivity without changing production bytecode, APIs, storage, or economics.

## Pull request and CI

Ready-for-review PR [#194](https://github.com/zkp2p/zkp2p-contracts/pull/194) was opened from `codex/foundry-test-migration`. Branch protection currently reports no classic protection rule; merge will still wait for all relevant checks and required approvals. The replacement `Foundry CI` workflow triggers on every push and every pull request to `main` with no feature-branch allowlist. It pins Node 20.20.2, Foundry v1.7.1, and every third-party action by commit SHA. Its jobs run the complete suite, deployment/package checks, the full coverage gate, and an observable Codecov v5 OIDC upload under the `foundry` flag. There are no fork tests or RPC-secret gates; all former live-head behavior was replaced with deterministic fixtures.

The initial push/PR runs are [29858136058](https://github.com/zkp2p/zkp2p-contracts/actions/runs/29858136058) and [29858189034](https://github.com/zkp2p/zkp2p-contracts/actions/runs/29858189034). Their build/package/localhost-deployment jobs passed in 2m41s and 3m41s. Their full-suite and full-IR coverage jobs failed three FFI deployment-parity tests because those two jobs had not copied the repository's safe `.env.default`; Hardhat rejected empty deploy-key configuration with `HH8` before the helper assertions ran. The build/deploy job already used the correct setup. The fix prepares the same local environment in every job. It also pins the coverage seed and limits failure output to the diagnostic tail while retaining complete local shard logs.

On pre-rebase head `fa61a86`, both [push run 29860616098](https://github.com/zkp2p/zkp2p-contracts/actions/runs/29860616098) and [pull-request run 29860619793](https://github.com/zkp2p/zkp2p-contracts/actions/runs/29860619793) were fully green. Build/package/localhost deployment completed in 2m51s on both; the complete suite took 36m07s and 36m15s; coverage plus Codecov took 7m35s and 7m37s; `codecov/patch` passed. These runs prove the CI design and environment fix but are intentionally treated as stale after rebasing onto `f6fd7aa`; fresh green runs on the rewritten head are required before merge.

Codex review raised two actionable findings on head `7993a46`: new production files could evade the baseline-derived denominator, and the StakeVault handler accepted every low-level revert. Both are resolved: current sources are discovered independently with a temporary-sentinel rejection test, and every handler action now predicts exact allowed selectors while two tests prove unexpected revert/success fail closed. Tightening the model exposed and fixed a test-only ghost-capacity bug where pending withdrawals were omitted. All three invariant seeds, the complete suite, and fixed-seed coverage pass after the fixes.

A subsequent P1 Codex review on head `e773ed4` correctly found that the coverage gate compared incompatible Hardhat/Istanbul and Foundry branch denominators. The replacement gate reconstructs the 1,512 starting executable behaviors, validates 1,383 exact mapped destinations plus six conservative collision additions, and compares them against the complete suite with identical Foundry minimal-IR anchors. Its first uninterrupted diagnostic run failed closed when signature-anchored inverse filters leaked parameterized native tests; signature-aware filters plus a mechanical filtered-enumeration equality check fixed the selector. The corrected same-denominator gate passes every aggregate and per-file requirement without changing production code or relaxing a threshold.

Exact-head push run [29875200234](https://github.com/zkp2p/zkp2p-contracts/actions/runs/29875200234) and pull-request run [29875202325](https://github.com/zkp2p/zkp2p-contracts/actions/runs/29875202325) are fully green on `2a5506f`. Build/package/deploy took 2m48s and 2m52s, the complete 1,528-test suite took 33m43s and 33m24s, and the corrected coverage plus Codecov jobs took 1h1m48s and 1h1m03s. Both OIDC uploads succeeded and `codecov/patch` passed. The exact-head Codex review's only remaining suggestion was to make coverage opt-in; it was resolved without a code change because the explicit Goal requires coverage on every push and pull request and therefore satisfies the repository instruction that coverage run only when the user explicitly requests it.

The final clean local deployment gate started a fresh `hardhat node --no-deploy` process and ran `corepack yarn deploy:localhost`; every active deployment script completed with exit 0 through the V3 payment-binding/risk-settlement topology. Generated localhost snapshot timestamps and payment-method churn were restored afterward and are not part of the branch. `forge fmt --check test-foundry` and `git diff --check` pass. Focused repository scans found zero `vm.skip`, `testFail`, empty test/invariant bodies, workflow `continue-on-error`, workflow path/event exclusions, obsolete Hardhat-test commands, or active test placeholders/log-only assertions. Historical placeholder and log-only findings remain quoted above only as baseline evidence.

The pre-review canonical `corepack yarn test` verification recompiled 53 touched test files with Solidity 0.8.18 via IR in 654.81s, then passed all 1,457 tests across 98 suites in 6.23s. After review hardening, the final warm command passed 1,459 tests across the same 98 suites in 5.90s with zero failures and zero skips. `corepack yarn build:ts:latest` and `corepack yarn pkg:test` passed; the package result was 3 suites / 8 tests.

Post-rebase verification on `f6fd7aa` passes 1,528/1,528 tests across 103 suites with zero failures/skips. Three explicit complete-suite seeds took 5.14s, 5.58s, and 5.84s. All 66 standalone V3 tests also passed as isolated invocations. The immutable 1,517-row audit reports zero unmapped or missing destinations and 1,528 live tests, while the 67-row upstream delta audit reports 67 unique live destinations and zero omissions. A clean-cache pre-review run compiled 242 files in 908.37s and completed 1,526 tests end-to-end in 913.66s; the two subsequent fuzz properties pass focused and in every complete seed. The corrected same-denominator coverage gate and its exact selector preflight pass. `forge fmt --check`, `git diff --check`, Hardhat compile (17 Solidity files / 94 typings), package build, and package tests (3 suites / 8 tests) pass.

The first post-rebase localhost deployment correctly failed at the stale six-argument V2 deployment call, exposing that upstream `f6fd7aa` had restored the relayer-registry constructor argument. Restoring `deploy/14_deploy_v2_system.ts` exactly to current `origin/main` fixed the retained production deployment surface. A new block-zero node then completed every active script through the standalone V3/risk-settlement topology in 7.54s. Generated localhost timestamps/payment-method churn were restored; both `contracts/` and deployment tooling now have zero diff from current `origin/main`.

## Final merge verification

PR [#194](https://github.com/zkp2p/zkp2p-contracts/pull/194) merged at `4c35fd5db03fa7700d842da345b574d5b0e4ccf2`. A fresh fetch proved `origin/main` was that exact merge and contained head `2a5506f` as an ancestor; the canonical scripts and schema-v2 same-denominator evidence were present. This mandatory verification also found that the two upstream-added Hardhat sources under `test/orchestratorV3/` had remained after their 66 behaviors were reconciled into Foundry. They are removed in a focused follow-up so the final repository truly has no obsolete Hardhat contract-test surface. Goal completion remains pending that follow-up merge, a fresh fetched-main check proving `test/` is empty, and its post-merge workflow result.
